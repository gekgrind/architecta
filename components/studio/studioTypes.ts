export type {
  ContentNodeData,
  GeneratedContentMap,
  GeneratedVersion,
  SelectedNode,
  StudioGraph as GraphState,
  StudioGraphEdge as GraphEdge,
  StudioGraphNode as GraphNode,
  StudioMode,
  Suggestion,
} from "@/lib/domain";

export { isContentNodeData, isStudioGraph } from "@/lib/domain";
