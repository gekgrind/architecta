# Archived Auth Files

These files were archived during the shared-auth migration and are intentionally inactive.

- They preserve Architecta's former app-owned login, signup, callback, and OAuth code.
- Active auth now relies on shared Entrepreneuria credential-entry routes plus local session/profile checks only.
- Nothing inside this folder should be imported by the active application.
