// Shared by the connections start/callback routes. Lives outside the route
// modules because Next.js route files may only export route handlers/config.
export const STATE_COOKIE = "arch_oauth_state";
